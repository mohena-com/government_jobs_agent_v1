# V1.9.33 — Semantic Date Binding & Cross-Check

Hotfix included:
- Explicit application-window evidence now overrides stale/unverified structured date values.
- Application start/end are semantically bound before Qwen generation.
- Slide 5 deterministically receives the verified application window when Qwen omits it.
- Missing application dates receive a controlled fallback instead of an empty section.
- Fee-payment, exam, notification and admit-card dates remain separate from the application window.
